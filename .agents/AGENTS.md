# Porta Project Rules

## Repository & Git Rules
- Always use `stlim0727/porta` as the origin repository for GitHub operations (PRs, issues, git push).
- Default gh CLI repository is set to `stlim0727/porta`.
